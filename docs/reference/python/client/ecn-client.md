---
title: "ECNClient"
description: "Manage one explicitly configured MQTT v5 ECN connection."
sidebar: { order: 1 }
tableOfContents: true
---

<!-- Generated by scripts/generate_api_reference.py. DO NOT EDIT. Regenerate with make generate-reference. -->

> Part of [Client & lifecycle](/reference/python/client/) in the Python API reference.
>
> Related guidance: [concepts/lifecycle](/concepts/lifecycle/), [getting-started/preflight](/getting-started/preflight/), [how-to/cleanup](/how-to/cleanup/).
>
> Runnable examples: [preflight.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/examples/preflight.py).

```python
from picogrid_ecn_client import ECNClient
```

Manage one explicitly configured MQTT v5 ECN connection.

```python
class ECNClient: ...
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L95)

Use `ECNClient` with `async with` to manage its asynchronous lifecycle.

## `__init__`

Create a client without connecting or installing subscriptions.

```python
def __init__(config: ECNConfig) -> None
```

### Parameters

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| config | ECNConfig | — | Frozen connection and operation configuration. |

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L103)

## `config`

Return the frozen configuration supplied at construction.

```python
config: ECNConfig
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L150)

## `entities`

Return the stable typed entity interface.

```python
entities: Entities
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L164)

## `locations`

Return the stable typed location interface.

```python
locations: Locations
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L169)

## `tasks`

Return the stable typed task interface.

```python
tasks: Tasks
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L174)

## `clock`

Return the ECN-relative clock diagnostic interface.

```python
clock: Clock
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L155)

## `is_ready`

Return whether MQTT and every still-required subscription are ready.

```python
is_ready: bool
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L179)

## `status`

Return a secret-safe snapshot of the current connection state.

```python
status: ConnectionStatus
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L186)

## `wait_until_ready`

Wait for strict readiness within one caller-supplied deadline.

```python
async def wait_until_ready(*, timeout: float) -> ConnectionStatus
```

### Parameters

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| timeout | float | — | Positive finite maximum wait in seconds. |

### Returns

| Type | Description |
| --- | --- |
| ConnectionStatus | The first strictly ready connection snapshot. |

### Raises

| Exception | Description |
| --- | --- |
| AuthenticationError | If credentials are unavailable or rejected. |
| AuthorizationError | If connection or required-subscription access is denied. |
| ConfigurationError | If unchanged local configuration is invalid. |
| ConnectionError | If recovery becomes terminal for another connection reason. |
| NotReadyError | If the client has not started or is closing/closed. |
| ProtocolError | If recovery terminates on a protocol violation. |
| ResourceLimitError | If the broker rejects a required resource. |
| TimeoutError | If the caller's deadline expires first. |
| ValidationError | If `timeout` is not positive and finite. |

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L203)

## `connection_events`

Return a capacity-one latest-value stream of redacted connection status.

```python
def connection_events() -> EventStream[ConnectionStatus]
```

### Raises

| Exception | Description |
| --- | --- |
| NotReadyError | If client closing has begun or the client is closed. |

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L250)

## `notify_credentials_changed`

Wake credential-blocked recovery after an atomic credential repair.

```python
def notify_credentials_changed() -> None
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L279)

## `request_retry`

Wake scheduled transient recovery or revive transient retry exhaustion.

```python
def request_retry() -> None
```

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L294)

## `start`

Connect to MQTT v5 and wait for readiness.

```python
async def start() -> None
```

### Raises

| Exception | Description |
| --- | --- |
| AuthenticationError | If authentication material or broker authentication is rejected. |
| AuthorizationError | If the broker rejects connection authorization. |
| ConfigurationError | If local MQTT or TLS verification configuration is invalid. |
| ConnectionError | If MQTT readiness fails or times out. |
| NotReadyError | If the client is closing or already closed. |
| ProtocolError | If MQTT readiness terminates on a protocol violation. |
| ResourceLimitError | If the broker rejects a required connection resource. |

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L304)

## `preflight`

Run read-only checks and caller-requested subscription probes.

```python
async def preflight(
    *,
    subscription_probes: Collection[SubscriptionProbe] = (),
) -> PreflightReport
```

### Parameters

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| subscription_probes | Collection[SubscriptionProbe] | () | Exact bounded subscription probes to request. |

### Returns

| Type | Description |
| --- | --- |
| PreflightReport | A secret-safe report of the completed checks. |

### Raises

| Exception | Description |
| --- | --- |
| TimeoutError | If closing the probe transport exceeds the configured shutdown timeout. Check failures are reported in the returned report rather than raised. |

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L459)

## `close`

Close the client and remove owned resources.

```python
async def close() -> None
```

### Raises

| Exception | Description |
| --- | --- |
| ConnectionError | If a component fails during cleanup. |
| TimeoutError | If cleanup exceeds the shutdown timeout in seconds. |

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L529)

## `__aenter__`

Start the client and return it after readiness.

```python
async def __aenter__() -> ECNClient
```

### Returns

| Type | Description |
| --- | --- |
| ECNClient | This client, ready for entity, location, and task work. |

### Raises

| Exception | Description |
| --- | --- |
| AuthenticationError | If authentication material or broker authentication is rejected. |
| AuthorizationError | If the broker rejects connection authorization. |
| ConfigurationError | If local MQTT or TLS verification configuration is invalid. |
| ConnectionError | If MQTT readiness fails or times out. |
| NotReadyError | If the client is closing or already closed. |
| ProtocolError | If MQTT readiness terminates on a protocol violation. |
| ResourceLimitError | If the broker rejects a required connection resource. |

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L565)

## `__aexit__`

Close the client when leaving the asynchronous context.

```python
async def __aexit__(
    exc_type: type[BaseException] | None,
    exc_value: BaseException | None,
    traceback: TracebackType | None,
) -> None
```

### Parameters

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| exc_type | type[BaseException] \| None | — | Type of the propagated exception, or `None`. |
| exc_value | BaseException \| None | — | Propagated exception instance, or `None`. |
| traceback | TracebackType \| None | — | Traceback for the propagated exception, or `None`. |

### Raises

| Exception | Description |
| --- | --- |
| ConnectionError | If a component fails during cleanup. |
| TimeoutError | If cleanup exceeds the shutdown timeout in seconds. |

[Source · client.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/client.py#L584)
