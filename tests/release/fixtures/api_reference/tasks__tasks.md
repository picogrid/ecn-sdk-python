---
title: "Tasks"
description: "Register typed handlers and dispatch typed requests."
sidebar: { order: 37 }
tableOfContents: true
---

<!-- Generated by scripts/generate_api_reference.py. DO NOT EDIT. Regenerate with make generate-reference. -->

> Part of [Tasks](/reference/python/tasks/) in the Python API reference.
>
> Related guidance: [concepts/tasks](/concepts/tasks/), [how-to/receive-local-tasks](/how-to/receive-local-tasks/), [how-to/dispatch-local-tasks](/how-to/dispatch-local-tasks/), [quickstarts/effector-handler](/quickstarts/effector-handler/).
>
> Runnable examples: [receive_task.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/examples/receive_task.py), [dispatch_task.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/examples/dispatch_task.py), [receive_mesh_task.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/examples/receive_mesh_task.py), [dispatch_mesh_task.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/examples/dispatch_mesh_task.py).

```python
from picogrid_ecn_client import Tasks
```

Register typed handlers and dispatch typed requests.

```python
class Tasks: ...
```

[Source · interfaces/tasks.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/interfaces/tasks.py#L147)

## `register`

Register one async handler for an exact task request topic.

```python
async def register(
    *,
    entity_id: UUID,
    command: str,
    request_model: type[RequestT],
    handler: RequestTaskHandler[RequestT, ResultT] | ContextTaskHandler[RequestT, ResultT],
    result_model: type[ResultT],
) -> TaskRegistration
```

```python
async def register(
    *,
    entity_id: UUID,
    command: str,
    request_model: type[RequestT],
    handler: RequestTaskHandler[RequestT, BaseModel] | ContextTaskHandler[RequestT, BaseModel],
    result_model: None = None,
) -> TaskRegistration
```

### Parameters

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| entity_id | UUID | — | Entity that receives the command. |
| command | str | — | Command identifying the exact task request topic. |
| request_model | type[RequestT] | — | Model used to validate request payloads. |
| handler | RequestTaskHandler[RequestT, ResultT] \| ContextTaskHandler[RequestT, ResultT] | — | Async request-only or context-and-request callable. |
| result_model | type[ResultT] | None | Optional model used to validate handler results. |

### Returns

| Type | Description |
| --- | --- |
| TaskRegistration | The local registration and its request and result schemas. |

### Raises

| Exception | Description |
| --- | --- |
| ValidationError | If a model or handler signature is invalid, or a handler is already registered for this entity and command. |
| NotReadyError | If the client cannot register a handler. |
| AuthorizationError | If the broker rejects the request-topic subscription. |
| ConnectionError | If the transport fails while subscribing. |

[Source · interfaces/tasks.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/interfaces/tasks.py#L175)

## `unregister`

Remove a local task handler and release its exact subscription.

```python
async def unregister(registration: TaskRegistration) -> None
```

### Parameters

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| registration | TaskRegistration | — | Registration returned by `register`. |

### Raises

| Exception | Description |
| --- | --- |
| NotReadyError | If the client is not ready. |
| AuthorizationError | If the broker rejects the unsubscribe. |
| ProtocolError | If the broker returns a malformed UNSUBACK. |
| ConnectionError | If the transport fails while unsubscribing. |

[Source · interfaces/tasks.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/interfaces/tasks.py#L220)

## `send`

Dispatch one task request using the selected response mode.

```python
async def send(
    *,
    target_entity_id: UUID,
    target_integration: str,
    target_terminal_id: UUID | None = None,
    command: str,
    request: BaseModel,
    result_model: type[ResultT],
    timeout: float | None = None,
    mode: Literal[TaskMode.COMPLETE] = TaskMode.COMPLETE,
    expected_connection_generation: int | None = None,
) -> TaskResult[ResultT]
```

```python
async def send(
    *,
    target_entity_id: UUID,
    target_integration: str,
    target_terminal_id: UUID | None = None,
    command: str,
    request: BaseModel,
    result_model: None = None,
    timeout: float | None = None,
    mode: Literal[TaskMode.COMPLETE] = TaskMode.COMPLETE,
    expected_connection_generation: int | None = None,
) -> TaskResult[Never]
```

```python
async def send(
    *,
    target_entity_id: UUID,
    target_integration: str,
    target_terminal_id: UUID | None = None,
    command: str,
    request: BaseModel,
    result_model: type[BaseModel] | None = None,
    timeout: float | None = None,
    mode: Literal[TaskMode.ACKNOWLEDGMENT],
    expected_connection_generation: int | None = None,
) -> TaskAcknowledgement | TaskResult[BaseModel]
```

```python
async def send(
    *,
    target_entity_id: UUID,
    target_integration: str,
    target_terminal_id: UUID | None = None,
    command: str,
    request: BaseModel,
    result_model: type[BaseModel] | None = None,
    timeout: float | None = None,
    mode: Literal[TaskMode.FIRE_AND_FORGET],
    expected_connection_generation: int | None = None,
) -> DispatchReceipt
```

```python
async def send(
    *,
    target_entity_id: UUID,
    target_integration: str,
    target_terminal_id: UUID | None = None,
    command: str,
    request: BaseModel,
    result_model: type[BaseModel] | None = None,
    timeout: float | None = None,
    mode: TaskMode,
    expected_connection_generation: int | None = None,
) -> TaskDispatchResult
```

### Parameters

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| target_entity_id | UUID | — | Entity that receives the command. |
| target_integration | str | — | Integration owning the target entity. |
| target_terminal_id | UUID \| None | None | Optional exact terminal route for the target. |
| command | str | — | Command to dispatch. |
| request | BaseModel | — | Pydantic request payload. |
| result_model | type[ResultT] | None | Optional model used to validate a complete response. |
| timeout | float \| None | None | Optional response timeout in seconds. |
| mode | TaskMode | TaskMode.COMPLETE | Response behavior for the dispatch. `TaskMode.COMPLETE` waits for a result, `TaskMode.ACKNOWLEDGMENT` waits only for the receiver to accept the request, and `TaskMode.FIRE_AND_FORGET` publishes without waiting. |
| expected_connection_generation | int \| None | None | Optional ready-connection generation that must still be active at the publication boundary. When supplied, it must be a non-boolean integer of one or greater. A mismatch fails before publication. |

### Returns

| Type | Description |
| --- | --- |
| TaskResult[ResultT] \| TaskAcknowledgement \| TaskResult[BaseModel] \| DispatchReceipt | The acknowledgment, result, or dispatch receipt selected by `mode` and the responding status. |

### Raises

| Exception | Description |
| --- | --- |
| NotReadyError | If the client is not ready. |
| OutcomeUnknownError | If request send begins without a valid PUBACK, or a valid PUBACK is followed by a missing, disconnected, timed-out, malformed, or invalid correlated response. The error retains the safe delivery phase and generated task ID. The client does not retry automatically. |
| ValidationError | If a dispatch argument or the request payload is invalid, including an explicit self-target. |
| ResourceLimitError | If outstanding task operations are exhausted or a payload exceeds the configured maximum. |
| AuthorizationError | If the broker rejects the response subscription or the request publication. |
| ProtocolError | If the response SUBACK or a definite PUBACK result violates the supported acknowledgment contract. |
| DeliveryError | If response-subscription or publication setup fails before request send begins. A mismatch in `expected_connection_generation` raises this error with phase `NOT_SENT` before publication. The error carries the generated task ID. |

[Source · interfaces/tasks.py](https://github.com/picogrid/ecn-sdk-python/blob/v0.1.0/src/picogrid_ecn_client/interfaces/tasks.py#L313)
